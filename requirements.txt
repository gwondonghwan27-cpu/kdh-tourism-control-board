numpy>=1.26
pandas>=2.1
networkx>=3.2
matplotlib>=3.8
plotly>=5.18
streamlit>=1.57
streamlit-plotly-events>=0.0.6
pydantic>=2.5
opencv-python-headless>=4.9
google-genai>=1.0
pytest>=8.0

# Optional full EPANET/WNTR backend. The MVP ships with a deterministic fallback
# simulator so the demo remains runnable even when WNTR is unavailable.
# wntr>=1.3
